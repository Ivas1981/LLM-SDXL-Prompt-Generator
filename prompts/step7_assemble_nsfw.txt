Assemble previous steps into a single JSON for Stable Diffusion XL.

Use only these exact keys:
{
  "subject": "<literally {prompt} + remaining clothing tags only, max 6 words>",
  "pose": "<body position + expression, max 7 words>",
  "state": "<physical/emotional condition only, max 8 words>",
  "nudity": "<scene-motivated partial nudity tags, max 6 words>",
  "environment": "<location + time + weather + props, max 10 words>",
  "relationships": "<spatial layout only, max 8 words>",
  "lighting": "<physical lighting only, max 6 words>",
  "camera": "<camera + lens + angle + quality, max 8 words>"
}

Rules:
- "{prompt}" MUST be preserved as a placeholder for the user's own description (woman, age, hair color, body type, ethnicity). Do NOT replace {prompt} with any text.
- Use only English. Comma-separated tags only. NO sentences. NO periods.
- Preserve compound terms: "Canon EOS R5", "24-70mm f/2.8L", "shallow depth of field", "shirtless torso".
- NO cross-field repetition: no eye contact in both pose/relationships; no lighting in environment; no pose in subject; no nudity in state.
- subject = "{prompt}" + remaining clothing ONLY (if some clothes are removed by the scene).
- pose = body position + expression ONLY (no eye contact phrase).
- state = physical/emotional condition ONLY (no nudity, no clothing, no pose).
- nudity = scene-motivated partial nudity ONLY (e.g. "skirt lifted by wind", "top shifted aside", "bare shoulders", "bare chest"). Keep diegetic and natural.
- environment = location + time + weather + props ONLY.
- relationships = spatial relations ONLY.
- camera = technical camera/lens/angle/quality ONLY.
- NO quality-bait: "masterpiece", "best quality", "8k", "ultra detailed", "HDR", "UHD".
- TOTAL positive prompt target: ~75 words MAX for SDXL compatibility.
- IMPORTANT: Do NOT copy examples. Create original combinations.
Output only the JSON object, nothing else.