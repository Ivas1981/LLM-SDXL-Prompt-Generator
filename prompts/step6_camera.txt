Describe technical camera parameters for the scene.

Output must be valid JSON with exactly these fields:
- camera: camera body/type as ONE tag (e.g. "Canon EOS R5")
- lens: focal length + aperture as ONE tag (e.g. "24-70mm f/2.8L")
- angle: shooting angle as ONE tag (e.g. "low angle")
- quality: optical look as ONE tag (e.g. "shallow depth of field")

Rules:
- Use only English. Each field = ONE comma-separated tag.
- Standard capitalization for camera/lens names.
- WRONG: "canon, eos, r5" or "24-70mm, f/2.8L" or "low, angle".
- NO people, actions, poses, clothing, scene content.
- NO quality-bait: "masterpiece", "8k", "ultra detailed", "HDR", "UHD".
- IMPORTANT: Do NOT copy examples from instructions.
Output only the JSON object, nothing else.