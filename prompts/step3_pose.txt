Generate clothing and pose for the scene.

Output must be valid JSON with exactly these fields:
- clothing: comma-separated tags of what subject wears (max 5 tags)
- pose: body position + expression (max 8 words)
- eye_contact: "direct" or "toward_camera" or "visible"

Rules:
- Use only English. Comma-separated tags only. NO sentences.
- Eye contact must match the pose: standing = "direct", seated/kneeling/leaning = "toward_camera", prone/hidden = "visible".
- Do NOT describe hair, skin, age, ethnicity, body type.
- Do NOT put physical condition/nudity into clothing or pose (state step handles this).
- Preserve compound terms: do not split multi-word clothing or pose terms.
- IMPORTANT: Do NOT copy examples from instructions. Create original combinations.
Output only the JSON object, nothing else.